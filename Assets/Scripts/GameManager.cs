using System.Collections;
using System.Collections.Generic;
using System;
using System.IO;
using UnityEngine;
using IFix.Core;

public class GameManager : MonoBehaviour
{
    void Awake()
    {
        //// init ifix
        //var patchPath = "./Assets/Resources/Assembly-CSharp.patch.bytes";
        //Debug.LogFormat("#IFIX# Load: {0} \tExist: {1}", patchPath, File.Exists(patchPath));
        //if (File.Exists(patchPath))
        //{
        //    PatchManager.Load(new FileStream(patchPath, FileMode.Open));
        //}
    }

    void Update()
    {
        
    }
}
